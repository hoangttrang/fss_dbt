version: 2

# This is used to documentation on the database and the schema 
models: 
  - name: mbr_report
    description: "This model is used to generate Motive monthly MBR report that gets update daily."
  - name: ukg_current_employee_info
    description: "This model is used to generate current employee information from UKG, including active employees and their benefits and their demographics. This table unique identifier is employee_id and type."
  - name: ukg_hr_kpi
    description: this model contains ALL employees hired by us or from integration. This includes both active and unactive employees
